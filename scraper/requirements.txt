requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.2.2
supabase==2.4.6
python-dotenv==1.0.1
# Playwright is alleen nodig voor de Facebook-scraper (draait NIET in CI).
# Installeer die lokaal via: pip install -r requirements-facebook.txt

imagehash>=4.3
Pillow>=10.0
